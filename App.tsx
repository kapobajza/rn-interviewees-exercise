import { StyleSheet } from 'react-native';
import data from './data';

export default function App() {
  // You should use the imported data variable for FlatList's data prop
  // <FlatList data={data} />
  // Write your code here (replace null with your component(s))
  return null;
}

// Write your styles here
const styles = StyleSheet.create({});
